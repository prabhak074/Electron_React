{
  "name": "electron_react",
  "version": "1.0.0",
  "description": "electron react app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"electron .",
    "dev_watch": "set ELECTRON_START_URL=http://localhost:3000 && npx electronmon --trace-warnings ."
  },
  "keywords": [
    "electron",
    "reactjs"
  ],
  "author": "Prabhakaran G",
  "license": "MIT",
  "dependencies": {
    "electron": "^29.1.0"
  }
}
